---
import Shell from "@/layouts/shell.astro";

export interface Props {
  title: string;
}

const { title } = Astro.props;
---

<Shell title={title}>
  <div
    class="py-20 max-w-[70%] prose prose-lg mx-auto print:py-0 print:mx-10 print:max-w-full"
  >
    <slot />
  </div>
</Shell>
