---
import Shell from "@/layouts/shell.astro";

export interface Props {
  title: string;
}

const { title } = Astro.props;
---

<Shell title={title}>
  <div
    class="prose prose-lg mx-auto max-w-[70%] py-20 print:mx-10 print:max-w-full print:py-0"
  >
    <slot />
  </div>
</Shell>
