---
import Base from "@layouts/base.astro";

export interface Props {
  title: string;
}

const { title } = Astro.props;
---

<Base title={title}>
  <div class="pt-48 flex-1 flex flex-col items-center">
    <slot />
  </div>
</Base>
